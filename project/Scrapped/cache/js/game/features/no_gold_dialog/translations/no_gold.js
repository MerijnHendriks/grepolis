/* global DM, _ */
(function () {
	"use strict";

	DM.loadData({
		l10n: {
			no_gold_dialog : {
				tabs : [
					_("Tab 1")
				],
				window_title : _("Not enough gold"),
				get_gold_now : _("Get gold now!"),
				main_text : _("You don't own enough gold.")
			}
		}
	});
}());
